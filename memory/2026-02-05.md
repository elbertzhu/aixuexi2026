# 2026-02-05

## 项目状态: v0.3.1 (Teacher Dashboard UI)

### 关键动作
1.  **止血**: 强制将 Server AI Provider 切回 `mock` (防御 MiniMax 地域限制 400 错误)。
2.  **Agent 切换**: Agent 模型已切换为 `minimax/MiniMax-M2.1`。
3.  **交付物**: 
    *   `PROJECT_HANDOVER.md`: 项目交接文档已生成。
    *   `docs/RELEASE_NOTES.md`: 更新了 v0.3.1 发布说明。
    *   `client/AIXueXi.swiftpm`: 完成了 Teacher Dashboard UI 端到端 (Summary + Detail + RBAC 403)。
4.  **冻结区**: SwiftPM 入口已冻结 (README 更新)，禁止回退 `.xcodeproj`。

### 记忆点
*   **严禁**: 发布阶段禁止调用真实 AI (MiniMax/OpenRouter)，只允许 Mock。
*   **入口**: Client 入口锁定为 `client/AIXueXi.swiftpm`。
*   **债务**: Parent 看板 UI 未完成 (仅 API)。

### 待办
*   [ ] 进入 v0.3.2 (Teacher Dashboard 体验优化) -> 等待指令。
